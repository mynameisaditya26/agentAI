[project]
name = "agentaigenai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mynameisaditya26", email = "as8975630@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "langchain>=1.3.18",
    "langchain-groq>=1.1.3",
    "langchain-openai>=1.6.0",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
]

[project.scripts]
agentaigenai = "agentaigenai:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
